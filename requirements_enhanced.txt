# Core Flask dependencies
Flask==3.0.0
Werkzeug==3.0.1

# HTTP requests for Symposium API integration
requests==2.31.0

# System monitoring for health checks
psutil==5.9.6

# JSON handling (built-in, but explicit for clarity)
# json - built-in

# Environment and configuration
python-dotenv==1.0.0

# Optional: Rate limiting (if enabled in future)
Flask-Limiter==3.5.0

# Optional: CORS support (if enabled)
Flask-CORS==4.0.0

# Optional: Enhanced logging
colorlog==6.7.0

# Development dependencies (only install in dev)
# pytest==7.4.3
# pytest-flask==1.3.0
# black==23.9.1
# flake8==6.1.0

# Production WSGI server
gunicorn==21.2.0

# Security headers
secure==0.3.0